import React from "react";
import "../styles/Gallery.css";

export default function Gallery() {
  return (
    <div>
      this is the about page now known as Gallery
      <p>
        This is a simple React application that demonstrates the use of React
        Router for navigation. The application consists of a Home page, an About
        page, and a Contact page.
      </p>
    </div>
  );
}
